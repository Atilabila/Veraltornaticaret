# Codex CLI Audit & Validation Report - VERAL Metal Poster Pro
Generated: 2026-02-05

## 🎯 Validation Objective
This checklist is designed for automated validation of the "Desktop-on-Mobile" hybrid architecture (1600px forced viewport) and recent UI/Performance regressions fixes.

---

## 🛠 1. Architecture: Viewport Enforcement
Logic: The site MUST bypass standard responsive breakpoints and force 1600px width on all mobile devices.

- [ ] **Check `src/app/layout.tsx`**: Must contain the `fixViewport` IIFE script setting `width=1600` and dynamic `initial-scale`.
- [ ] **Check `src/app/mobile-fixes.css`**: Must contain `@layer base { html, body { width: 1600px !important; min-width: 1600px !important; } }`.
- [ ] **Check `src/app/layout.tsx`**: `Viewport` export must be `width: 1600`.

## 🎨 2. UI Readability (1600px Mobile Compliance)
Logic: Standard desktop fonts (14px-16px) are unreadable when scaled down by 0.25x. All critical text must be bumped.

- [ ] **Check `src/app/mobile-fixes.css`**:
    - [ ] `h1` must be `~90px !important`.
    - [ ] `header nav a` must be `~32px !important`.
    - [ ] `footer` links must be `~30px !important`.
    - [ ] `.product-grid h3` must be `~36px !important`.
    - [ ] `.product-detail-page h1` must be `~68px !important`.

## ⚡ 3. Performance & Rendering
Logic: Forcing 1600px on mobile causes high GPU load. Optimization is mandatory.

- [ ] **Hardware Acceleration**: Search for `transform: translateZ(0)` in `mobile-fixes.css` on `section`, `.stats-marquee`, `.instagram-marquee`.
- [ ] **CSS Containment**: Check `main.home-page` for `contain: layout paint` and `section` for `contain: content`.
- [ ] **Reduced Overhead**: 
    - [ ] Check `src/components/layout/GlobalGrid.tsx` for `backgroundSize: '100px 100px'`.
    - [ ] Check `src/components/sections/ProductGallery.tsx` for removal of `backdrop-blur` and heavy `whileInView` animations.

## 🐞 4. Bug Fix Verification (MP-CORE-001)
- [ ] **SKU Removal**: Open `src/components/product/ProductCard.tsx` - Verify `SystemLabel` (SKU/ID) is removed or commented out.
- [ ] **Conditional Hide**: Open `src/components/sections/ProcessSection.tsx` - Verify "HASSAS İŞÇİLİK" card has class `hide-on-mobile-force`.
- [ ] **Grid Integrity**: Open `src/app/mobile-fixes.css` - Check `section#process .grid` has `grid-template-columns: repeat(4, 1fr) !important` to account for hidden item.

## 📦 5. Final Build & Integration
- [ ] **Build Check**: Run `npm run build`. Exit code must be `0`.
- [ ] **Style Leak Check**: Verify `mobile-fixes.css` does NOT have encoding errors (corrupted character spaces).
- [ ] **Client State**: Verify `ProductDetailClient.tsx` has class `product-detail-page`.

---
## 🏁 Conclusion
The system is currently in a **"High Performance / forced 1600px Readability"** state. Do NOT revert `!important` flags in `mobile-fixes.css` as they are required to override system inline styles generated by various Framer Motion components.
