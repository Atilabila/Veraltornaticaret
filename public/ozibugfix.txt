CODEx — TEK GÖREV: Repo’da arayıp bul, düzelt, SADECE git diff ver.

Proje: Next.js (App Router) + TailwindCSS.
Hedef: 1) Ana sayfa HERO sağ görsel davranışı 2) /urunler ürün kart görselleri davranışı.

KURAL: Çıktın sadece tek bir unified git diff (```diff ... ```) + en sonda 3 maddelik “How to verify” olacak. Tavsiye/yorum yok.

ÖNCE DOSYALARI BUL
- ripgrep ile arat ve gerçek componentleri tespit et:
  - "METAL ÜRETİM" 
  - "KATALOG BAŞLAT"
  - "/urunler" veya "urunler" route
  - "grayscale" 
  - "hover:grayscale-0"
  - "ProductCard" / "ProductGrid" / "Catalog" benzeri
- Hero sağdaki görselin <Image>/<img> satırını ve ürün kartı görsellerini render eden yeri bul.

İSTENEN DAVRANIŞ (NET)
A) HERO SAĞ GÖRSEL:
- WEB (hover destekli cihaz): varsayılan SİYAH-BEYAZ kalsın. (İstersen hover’da renklenebilir; tercih: hover’da renklenme kalsın)
- MOBİL (touch/hover yok): varsayılan RENKLİ olsun.
- Mevcut `grayscale`, `hover:grayscale-0`, `filter` class’ları yanlışsa kaldır/yeniden düzenle.
- Uygulama tercihi: Tailwind arbitrary media variant kullan:
  - mobil default: `filter-none grayscale-0`
  - sadece hover cihazda: `[@media(hover:hover)_and_(pointer:fine)]:grayscale`
  - hover’da renklenme istiyorsan: `[@media(hover:hover)_and_(pointer:fine)]:hover:grayscale-0`
- Eğer projede global CSS ile img’lere grayscale veriliyorsa bunu da düzelt (mobilde filter none, webde koşullu).

B) /URUNLER ÜRÜN KART GÖRSELLERİ:
- WEB + MOBİL: HER ZAMAN RENKLİ olacak. Siyah-beyaz yok.
- Ürün görsellerinde veya parent’larında bulunan tüm grayscale/filter kurallarını kaldır.
- En garanti: ürün görseline `filter-none grayscale-0` uygula ve varsa global grayscale’i override et.

C) REGRESYON KONTROL
- Diğer sayfalardaki görselleri bozma: sadece hero sağ görsel koşullu, ürün kart görselleri tamamen renkli.
- Tailwind class çatışmalarını temizle (aynı elementte hem grayscale hem grayscale-0 bırakma).

SONUÇ
- Gerekirse globals.css (veya tailwind layer utilities) ekle.
- Değiştirdiğin dosyalar: hero component + ürün kart component + (gerekirse) globals.css.
- Sadece diff ver, sonra 3 maddelik doğrulama listesi:
  1) mobilde hero renkli
  2) webde hero siyah-beyaz (hover’da renkleniyorsa renklenir)
  3) ürün kart görselleri web+mobil renkli
