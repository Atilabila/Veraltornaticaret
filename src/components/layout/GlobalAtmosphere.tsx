// This component is intentionally empty.
// The ambient background effect is now handled purely via CSS in globals.css
// This approach is much more performant and doesn't require JavaScript.

export const GlobalAtmosphere = () => null;
